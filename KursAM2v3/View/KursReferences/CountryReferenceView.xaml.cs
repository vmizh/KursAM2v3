using System.Windows;
using DevExpress.Xpf.Grid;
using LayoutManager;

namespace KursAM2.View.KursReferences
{
    /// <summary>
    ///     Interaction logic for CountryReferenceView.xaml
    /// </summary>
    public partial class CountryReferenceView : ILayout
    {
        public CountryReferenceView()
        {
            InitializeComponent();
            LayoutManager = new LayoutManager.LayoutManager(GetType().Name, this, gridControl);
            Loaded += Layout_Loaded;
            Unloaded += Layout_Unloaded;
        }

        public LayoutManagerBase LayoutManager { get; set; }
        public string LayoutManagerName { get; set; }

        private void Layout_Loaded(object sender, RoutedEventArgs e)
        {
            LayoutManager.Load();
        }

        private void Layout_Unloaded(object sender, RoutedEventArgs e)
        {
            LayoutManager.Save();
        }

        private void GridControl_OnAutoGeneratedColumns(object sender, RoutedEventArgs e)
        {

        }

        private void GridControl_OnAutoGeneratingColumn(object sender, AutoGeneratingColumnEventArgs e)
        {
            e.Column.Name = e.Column.FieldName;
            switch (e.Column.FieldName)
            {
                case "Sign2":
                    //e.Column.
                    break;
            }
        }
    }
}