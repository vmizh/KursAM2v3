<UserControl x:Class="KursAM2.View.Management.Controls.BalansCompareKontragentUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:KursAM2.View.Management.Controls"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <dxg:GridControl
        x:Name="gridKontragent"
        AutoGenerateColumns="AddNew"
        EnableSmartColumnsGeneration="True"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        dx:DXSerializer.SerializationID="gridKontragent"
        dx:DXSerializer.LayoutVersion="1"
        dx:DXSerializer.StoreLayoutMode="All"
        dxg:DataControlSerializationOptions.AddNewColumns="True"
        dxg:DataControlSerializationOptions.RemoveOldColumns="True"
        ItemsSource="{Binding KontragentDeltaList}"
        CurrentItem="{Binding CurrentKontragent}"
        AutoGeneratedColumns="OnColumnsGenerated"
        AutoGeneratingColumn="GridKontragent_OnAutoGeneratingColumn">
        <dxg:GridControl.DetailDescriptor>
            <dxg:ContentDetailDescriptor x:Name="descriptor">
                <dxg:ContentDetailDescriptor.ContentTemplate>
                    <DataTemplate>
                        <Grid>
                            <local:BalansComareKontragentOperationsUI x:Name="detail" />
                        </Grid>
                    </DataTemplate>
                </dxg:ContentDetailDescriptor.ContentTemplate>
            </dxg:ContentDetailDescriptor>
        </dxg:GridControl.DetailDescriptor>
        <dxg:GridControl.View>
            <dxg:TableView x:Name="gridViewKontragent"
                           
                           
                           AllowPerPixelScrolling="True"
                           ShowTotalSummary="True">
                <dxg:TableView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Открыть лицевой счет" 
                                  Command="{Binding KontragentBalansOpenCommand}"/>
                        <MenuItem Header="Экспорт"
                                  Command="{Binding VisualControlExportCommand}"
                                  CommandParameter="{Binding Path=PlacementTarget, RelativeSource={RelativeSource Mode=FindAncestor,
                                              AncestorType={x:Type ContextMenu}}}">
                            <MenuItem.Icon>
                                <Image
                                    Source="pack://siteoforigin:,,,/Images/export1.png" />
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </dxg:TableView.ContextMenu>
            </dxg:TableView>
        </dxg:GridControl.View>
    </dxg:GridControl>
</UserControl>