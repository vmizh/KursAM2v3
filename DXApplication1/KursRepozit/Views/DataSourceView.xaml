<UserControl xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  
             x:Class="KursRepozit.Views.DataSourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
   <Grid>
        <dxg:GridControl Name="gridControlDataSource"
                         AutoGenerateColumns="AddNew" 
                         EnableSmartColumnsGeneration="True" 
                         HorizontalAlignment="Stretch" 
                         VerticalAlignment="Stretch"
                         SelectionMode="Cell"
                         ItemsSource="{Binding DataSources}"
                         CurrentItem="{Binding CurrentDataSource,UpdateSourceTrigger=PropertyChanged}"
                         AutoGeneratingColumn="GridControlDataSource_OnAutoGeneratingColumn"
                         AutoGeneratedColumns="GridControlDataSource_OnAutoGeneratedColumns"
                         >
            <dxg:GridControl.View>
                <dxg:TableView 
                    AllowPerPixelScrolling="True"
                    ShowGroupPanel="False"
                    ShowTotalSummary="True"
                    ShowAutoFilterRow="True"
                    EnableImmediatePosting="True">
                    <dxg:TableView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Добавить источник" Command="{Binding AddSourceCommand}"/>
                            <MenuItem Header="Удалить источник" Command="{Binding DeleteSourceCommand}"/>
                            <Separator/>
                            <MenuItem Header="Экспорт"
                                      Command="{Binding VisualControlExportCommand}"
                                      CommandParameter="{Binding Path=PlacementTarget, RelativeSource={RelativeSource Mode=FindAncestor,
                                              AncestorType={x:Type ContextMenu}}}"/>
                        </ContextMenu>
                    </dxg:TableView.ContextMenu>
                </dxg:TableView>
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</UserControl>
